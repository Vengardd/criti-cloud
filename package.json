{
  "name": "criti-cloud",
  "version": "1.0.0",
  "description": "Projekt z React i Java",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose up --build",
    "start:dev": "docker-compose up",
    "backend": "docker compose up --build backend",
    "frontend": "npm --prefix ./criti-cloud-front run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "author": "CritiCloud",
  "license": "MIT"
}
